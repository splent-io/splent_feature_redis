services:

  splent_feature_redis:
    image: redis:latest
    restart: always
    ports:
      - "${REDIS_HOST_PORT}:6379"
    networks:
      - splent_network

networks:

  splent_network:
    driver: bridge
    name: splent_network
